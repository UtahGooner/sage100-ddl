{
  "name": "sage100-ddl",
  "version": "1.0.2",
  "scripts": {
    "version": "auto-changelog -p && git add CHANGELOG.md"
  },
  "devDependencies": {
    "auto-changelog": "^2.4.0"
  },
  "dependencies": {},
  "auto-changelog": {
    "template": "keepachangelog",
    "unreleased": true,
    "commitLimit": false
  }
}
